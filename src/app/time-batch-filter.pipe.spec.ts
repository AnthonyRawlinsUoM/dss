import { TimeBatchFilterPipe } from './time-batch-filter.pipe';

describe('TimeBatchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeBatchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
